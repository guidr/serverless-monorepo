{
  "name": "@rooster/countries",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf ./dist tsconfig.tsbuildinfo",
    "lint": "tslint --project tsconfig.json",
    "test": "echo \"Error: run tests from root\" && exit 1",
    "watch": "tsc -w"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "tslint": "^5.20.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.31",
    "@types/faker": "^4.1.5",
    "faker": "^4.1.0",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  }
}
